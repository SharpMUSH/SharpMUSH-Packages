package: hello-world
version: 1.0.0
authors: [SharpMUSH]
description: "Minimal example package: one object, one command."

objects:
  - ref: hello_global
    type: thing
    name: Hello World Object
    attributes:
      CMD_+HELLO: "$+hello:@pemit %#=Hello, [name(%#)]!"
